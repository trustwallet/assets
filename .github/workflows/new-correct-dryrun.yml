name: Corrections - Dry run
on:
    push:
      branches:
        - '*'
        - '!master'
    pull_request:
      branches: [master]
jobs:
  correct-dryrun:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v2
     - uses: actions/setup-node@v1
       with:
         node-version: 12
     - name: Install Dependencies
       run: npm ci
     - name: Run correct script
       run: npm run new-correct
     - name: Run test
       run: npm t
     - name: Show git status
       if: success()
       run: git status
