import { prisma } from '@/lib/prisma';
export default async function Page(){const [automations,runs]=await Promise.all([prisma.workflowAutomation.findMany({orderBy:{createdAt:'desc'}}),prisma.workflowRun.findMany({take:20,include:{automation:true},orderBy:{createdAt:'desc'}})]);return <section><h2 className="text-3xl font-bold">Workflow Automation</h2><p className="mt-2 text-slate-600">n8n-ready event workflows for leads, profile updates, visa changes, property handovers, and document expiry.</p><div className="mt-6 grid gap-4 lg:grid-cols-2"><div className="card p-5"><h3 className="font-bold">Automations</h3>{automations.map(a=><p className="mt-3" key={a.id}>{a.name}<br/><span className="text-sm text-slate-500">{a.eventType} · {a.enabled?'Enabled':'Disabled'} · {a.webhookUrl??'No webhook'}</span></p>)}</div><div className="card p-5"><h3 className="font-bold">Recent Runs</h3>{runs.map(r=><p className="mt-3" key={r.id}>{r.automation.name}<br/><span className="text-sm text-slate-500">{r.status} · {r.createdAt.toISOString()}</span></p>)}</div></div></section>}
