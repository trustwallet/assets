import { prisma } from '@/lib/prisma';import { redirect } from 'next/navigation';import { Input,Textarea } from '@/components/ui/input';import { Button } from '@/components/ui/button';
async function create(form:FormData){'use server';await prisma.emailTemplate.create({data:{name:String(form.get('name')),subject:String(form.get('subject')),category:String(form.get('category')),body:String(form.get('body'))}});redirect('/dashboard/email-templates')}
export default async function Page(){const templates=await prisma.emailTemplate.findMany({orderBy:{name:'asc'}});return <section><h2 className="text-3xl font-bold">Email Template Manager</h2><form action={create} className="card mt-6 grid gap-3 p-5"><div className="grid gap-3 md:grid-cols-3"><Input name="name" placeholder="Template name" required/><Input name="subject" placeholder="Subject" required/><Input name="category" placeholder="Category" required/></div><Textarea name="body" placeholder="Use variables like {{first_name}}, {{preferred_area}}, {{budget}}" required/><Button>Add Template</Button></form><div className="mt-6 grid gap-4 md:grid-cols-2">{templates.map(t=><article className="card p-5" key={t.id}><p className="text-xs uppercase text-brand-700">{t.category}</p><h3 className="font-bold">{t.name}</h3><p className="text-sm text-slate-500">{t.subject}</p><pre className="mt-3 whitespace-pre-wrap rounded-lg bg-slate-50 p-3 text-sm">{t.body}</pre></article>)}</div></section>}
