import { prisma } from '@/lib/prisma';
export default async function Page(){const items=await prisma.contentItem.findMany({orderBy:{createdAt:'desc'}});return <section><h2 className="text-3xl font-bold">Content Management System</h2><p className="mt-2 text-slate-600">Blog articles, area guides, hospital guides, developer reviews, market reports, visa updates, and retirement guides.</p><div className="mt-6 grid gap-4 md:grid-cols-2">{items.map(i=><article className="card p-5" key={i.id}><p className="text-xs text-brand-700">{i.type} · {i.region??'All regions'}</p><h3 className="font-bold">{i.title}</h3><p className="text-sm text-slate-600">{i.seoTitle??i.excerpt}</p></article>)}</div></section>}
