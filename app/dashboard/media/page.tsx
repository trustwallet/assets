import { prisma } from '@/lib/prisma';export default async function Page(){const media=await prisma.propertyMedia.findMany({include:{property:true},orderBy:{createdAt:'desc'}});return <section><h2 className="text-3xl font-bold">Media Library</h2><div className="mt-6 grid gap-4 md:grid-cols-3">{media.map(m=><article className="card overflow-hidden" key={m.id}>{m.mediaType==='IMAGE'&&<img className="h-44 w-full object-cover" src={m.fileUrl} alt={m.title??''}/>}<div className="p-4"><b>{m.title??m.mediaType}</b><p className="text-sm text-slate-500">{m.property.propertyCode}</p></div></article>)}</div></section>}
