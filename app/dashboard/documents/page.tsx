import { prisma } from '@/lib/prisma';
export default async function Page(){const docs=await prisma.document.findMany({include:{retiree:true},orderBy:{createdAt:'desc'}});return <section><h2 className="text-3xl font-bold">Document Repository</h2><form action="/api/documents" method="post" encType="multipart/form-data" className="card mt-6 grid gap-3 p-5 md:grid-cols-3"><input className="rounded-lg border p-2" name="title" placeholder="Title"/><select className="rounded-lg border p-2" name="documentType"><option>PASSPORT</option><option>VISA_DOCUMENT</option><option>PROPERTY_CONTRACT</option><option>PAYMENT_RECEIPT</option><option>INSURANCE_POLICY</option></select><input className="rounded-lg border p-2" type="file" name="file"/><button className="rounded-lg bg-brand-700 px-4 py-2 text-white">Secure Upload</button></form><div className="card mt-6 overflow-x-auto"><table className="w-full text-left text-sm"><tbody>{docs.map(d=><tr className="border-b" key={d.id}><td className="p-3 font-semibold">{d.title}</td><td>{d.documentType}</td><td>{d.retiree?`${d.retiree.firstName} ${d.retiree.lastName}`:'—'}</td><td>{d.expiryDate?.toISOString().slice(0,10)??'No expiry'}</td><td>v{d.version}</td></tr>)}</tbody></table></div></section>}
