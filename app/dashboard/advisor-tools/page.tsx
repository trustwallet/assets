import { prisma } from '@/lib/prisma';
export default async function Page(){const assets=await prisma.advisorAsset.findMany({include:{retiree:true},orderBy:{createdAt:'desc'}});return <section><h2 className="text-3xl font-bold">Advisor Productivity Suite</h2><p className="mt-2 text-slate-600">Generate follow-up emails, property comparisons, client summaries, consultation notes, relocation plans, investment reports, meeting summaries, and visa checklists.</p><div className="card mt-6 p-5">{assets.map(a=><div className="border-b py-3" key={a.id}><b>{a.assetType}</b> — {a.title}<p className="text-sm text-slate-600">{a.content}</p><span className="text-xs text-slate-500">{a.retiree?`${a.retiree.firstName} ${a.retiree.lastName}`:'No client'}</span></div>)}</div></section>}
