import { prisma } from '@/lib/prisma';
export default async function Page(){const partners=await prisma.partner.findMany({include:{referrals:true},orderBy:{name:'asc'}});return <section><h2 className="text-3xl font-bold">Partner Management</h2><div className="mt-6 grid gap-4 md:grid-cols-3">{partners.map(p=><article className="card p-5" key={p.id}><p className="text-xs text-brand-700">{p.type}</p><h3 className="font-bold">{p.name}</h3><p>{p.region??p.country??'Global'}</p><p>{p.referrals.length} referrals · ฿{p.referrals.reduce((s,r)=>s+Number(r.revenueGenerated??0),0).toLocaleString()} revenue</p></article>)}</div></section>}
