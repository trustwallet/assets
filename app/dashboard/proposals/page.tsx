import { prisma } from '@/lib/prisma';
export default async function Page(){const proposals=await prisma.clientProposal.findMany({include:{retiree:true},orderBy:{createdAt:'desc'}});return <section><h2 className="text-3xl font-bold">AI Client Proposal Generator</h2><div className="mt-6 grid gap-4 md:grid-cols-2">{proposals.map(p=><article className="card p-5" key={p.id}><h3 className="font-bold">{p.title}</h3><p>{p.retiree.firstName} {p.retiree.lastName} · {p.status}</p><p className="text-sm text-slate-500">Includes client profile, areas, properties, visa strategy, healthcare, investment analysis, and relocation plan.</p></article>)}</div></section>}
