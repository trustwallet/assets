import { prisma } from '@/lib/prisma';
export default async function Page(){const clients=await prisma.retiree.findMany({include:{shortlists:true,aiRecommendations:true,visaWorkflows:true,tasks:true,documents:true,appointments:true,communications:true,portfolioHoldings:true},take:12,orderBy:{updatedAt:'desc'}});return <section><h2 className="text-3xl font-bold">Client Portal Overview</h2><p className="mt-2 text-slate-600">Secure portal architecture: each client reads only their profile, properties, shortlist, recommendations, visa progress, tasks, documents, appointments, communications, and investment performance.</p><div className="mt-6 grid gap-4 md:grid-cols-2">{clients.map(c=><article className="card p-5" key={c.id}><h3 className="font-bold">{c.firstName} {c.lastName}</h3><div className="mt-3 grid grid-cols-4 gap-2 text-center text-sm"><span>{c.shortlists.length}<br/>Shortlist</span><span>{c.visaWorkflows.length}<br/>Visas</span><span>{c.documents.length}<br/>Docs</span><span>{c.portfolioHoldings.length}<br/>Assets</span></div></article>)}</div></section>}
