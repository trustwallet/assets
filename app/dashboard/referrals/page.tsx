import { prisma } from '@/lib/prisma';
export default async function Page(){const refs=await prisma.referral.findMany({include:{referredRetiree:true},orderBy:{createdAt:'desc'}});return <section><h2 className="text-3xl font-bold">Referral Program</h2><div className="card mt-6 overflow-x-auto"><table className="w-full text-left text-sm"><tbody>{refs.map(r=><tr className="border-b" key={r.id}><td className="p-3">{r.type}</td><td>{r.referrerName}</td><td>{r.referredRetiree?`${r.referredRetiree.firstName} ${r.referredRetiree.lastName}`:'—'}</td><td>฿{Number(r.revenue??0).toLocaleString()}</td><td>{r.status}</td></tr>)}</tbody></table></div></section>}
