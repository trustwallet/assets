import { prisma } from '@/lib/prisma';
export default async function Dashboard(){const [projects,properties,avg,areas,units,handovers]=await Promise.all([prisma.project.count(),prisma.property.count(),prisma.propertyScores.aggregate({_avg:{retirementScore:true}}),prisma.project.groupBy({by:['location'],_count:true,take:8}),prisma.project.aggregate({_sum:{unitsAvailable:true}}),prisma.project.findMany({where:{handoverDate:{gte:new Date()}},take:6,orderBy:{handoverDate:'asc'}})]);return <section><h2 className="text-3xl font-bold">Inventory Dashboard</h2><div className="mt-6 grid gap-4 md:grid-cols-3">{[['Total Projects',projects],['Total Properties',properties],['Average Retirement Score',Math.round(avg._avg.retirementScore??0)],['Units Available',units._sum.unitsAvailable??0],['Active Areas',areas.length],['Upcoming Handovers',handovers.length]].map(([k,v])=><div className="card p-5" key={k}><p className="text-sm text-slate-500">{k}</p><p className="mt-2 text-3xl font-bold">{v}</p></div>)}</div><div className="mt-6 grid gap-4 lg:grid-cols-2"><div className="card p-5"><h3 className="font-bold">Properties by Area</h3>{areas.map(a=><p className="mt-3 flex justify-between"><span>{a.location}</span><b>{a._count}</b></p>)}</div><div className="card p-5"><h3 className="font-bold">Upcoming Handovers</h3>{handovers.map(h=><p className="mt-3 flex justify-between"><span>{h.projectName}</span><span>{h.handoverDate?.toISOString().slice(0,10)}</span></p>)}</div></div></section>}
