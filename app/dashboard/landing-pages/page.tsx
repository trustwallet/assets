import { prisma } from '@/lib/prisma';
export default async function Page(){const pages=await prisma.landingPage.findMany({orderBy:{createdAt:'desc'}});return <section><h2 className="text-3xl font-bold">SEO Landing Page Engine</h2><div className="mt-6 grid gap-4 md:grid-cols-2">{pages.map(p=><article className="card p-5" key={p.id}><p className="text-xs text-brand-700">/{p.slug}</p><h3 className="font-bold">{p.headline}</h3><p>{p.region??'Multi-region'} · {p.propertyType??'All property types'}</p><p className="text-sm text-slate-500">{p.seoTitle}</p></article>)}</div></section>}
