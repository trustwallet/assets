import { prisma } from '@/lib/prisma';
export default async function Page(){const retirees=await prisma.retiree.findMany({include:{propertyPreference:true,financialProfile:true},orderBy:{updatedAt:'desc'}});return <section><h2 className="text-3xl font-bold">Match Results Dashboard</h2><p className="mt-2 text-slate-600">Select a retiree to generate top-10 property matches with budget, location, healthcare, airport, safety, lifestyle, and expat-community scoring.</p><div className="mt-6 grid gap-4 md:grid-cols-2">{retirees.map(r=><a className="card block p-5 hover:border-brand-500" href={`/dashboard/retirees/${r.id}/matches`} key={r.id}><h3 className="font-bold">{r.firstName} {r.lastName}</h3><p className="text-sm text-slate-500">฿{Number(r.financialProfile?.investmentBudget??0).toLocaleString()} · {r.propertyPreference?.preferredLocations.join(', ')||'No preferred area'}</p></a>)}</div></section>}
