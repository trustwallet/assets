import { prisma } from '@/lib/prisma';
export default async function Page(){const comms=await prisma.communication.findMany({include:{retiree:true},orderBy:{createdAt:'desc'},take:50});return <section><h2 className="text-3xl font-bold">Communication Center</h2><p className="mt-2 text-slate-600">Email, call, meeting, WhatsApp-ready, and note timeline.</p><div className="card mt-6 p-5">{comms.map(c=><div className="border-b py-3" key={c.id}><b>{c.type}</b> {c.subject??''}<p>{c.body}</p><span className="text-sm text-slate-500">{c.retiree?`${c.retiree.firstName} ${c.retiree.lastName}`:'—'} · {c.createdAt.toISOString()}</span></div>)}</div></section>}
