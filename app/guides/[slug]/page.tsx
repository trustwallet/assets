import { prisma } from '@/lib/prisma';
export default async function Page({params}:{params:{slug:string}}){const magnet=await prisma.leadMagnet.findUniqueOrThrow({where:{slug:params.slug}});return <main className="mx-auto max-w-3xl p-6"><h1 className="text-4xl font-bold">{magnet.title}</h1><p className="mt-3 text-slate-600">{magnet.description}</p><form action={`/api/lead-magnets/${magnet.slug}/download`} method="post" className="card mt-6 grid gap-3 p-5"><input name="name" placeholder="Name" className="rounded-lg border p-2"/><input name="email" placeholder="Email" className="rounded-lg border p-2"/><input name="country" placeholder="Country" className="rounded-lg border p-2"/><input name="timeline" placeholder="Relocation timeline" className="rounded-lg border p-2"/><button className="rounded-lg bg-brand-700 px-4 py-2 text-white">Download Guide</button></form></main>}
