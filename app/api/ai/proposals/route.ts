import { prisma } from '@/lib/prisma';import { NextResponse } from 'next/server';
export async function POST(req:Request){const {retireeId}=await req.json();const retiree=await prisma.retiree.findUniqueOrThrow({where:{id:retireeId},include:{propertyPreference:true,aiRecommendations:{include:{property:true}},visaWorkflows:true}});const editableContent={clientProfile:retiree,recommendedAreas:retiree.propertyPreference?.preferredLocations??[],recommendedProperties:retiree.aiRecommendations.slice(0,5),visaStrategy:retiree.visaWorkflows,healthcareRecommendations:['Review hospital proximity','Confirm insurance coverage'],investmentAnalysis:['Compare yield and appreciation'],relocationPlan:['Visa','Property shortlist','Viewing trip','Offer','Move']};const proposal=await prisma.clientProposal.create({data:{retireeId,title:`TRLA Proposal for ${retiree.firstName} ${retiree.lastName}`,editableContent}});return NextResponse.json(proposal,{status:201})}
