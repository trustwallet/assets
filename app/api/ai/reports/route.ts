import { prisma } from '@/lib/prisma';import { NextResponse } from 'next/server';
export async function POST(req:Request){const {retireeId,reportType='RETIREMENT_SUITABILITY'}=await req.json();const retiree=retireeId?await prisma.retiree.findUnique({where:{id:retireeId},include:{propertyPreference:true,visaWorkflows:true,portfolioHoldings:true}}):null;const content={summary:`AI-generated ${reportType} report`,retiree,sections:['Executive summary','Recommendations','Risks','Next steps']};const report=await prisma.aiGeneratedReport.create({data:{retireeId,reportType,title:`${reportType} Report`,content}});return NextResponse.json(report,{status:201})}
