# .claude/repo.yml — machine-readable repo manifest for AI agents.
# Generated by sdd-knowledge; refreshed by `sdd-knowledge --garden`.
# Agents read this to understand what the repo is for and to decide
# whether a task belongs here. Cross-repo routers match a task against
# many repos' `owns` / `topics` / `summary` to pick the right target repo.
#
# Curated fields (owns, topics, consumers, status) are PRESERVED across
# garden runs — edit them freely. Mechanical fields (name, summary, domain,
# ships, stack, depends_on, verified) are regenerated from repo signals.
# `summary` is synthesized from CLAUDE.md "## What this repo is" + every
# knowledge/ doc's frontmatter; edits to it are overwritten on the next run.
schemaVersion: 1
name: assets
summary: |-
  This repository is documented by 8 classified knowledge entries spanning 4 categories.

  Knowledge base: 8 classified documents across 4 categories — ci (3), patterns (2), specs (2), code-conventions (1). Documentation types: how-to (5), reference (2), tutorial (1). Confidence: low (5), medium (2), high (1). Scope: org (8) (org = generic/reusable, repo = project-specific). Most-referenced topics: make, run, anti-pattern, logo, partnership, pattern, script, use case.

  Coverage by area: ci (3 docs, subcategories: workflows; key topics: make, run, script); patterns (2 docs, subcategories: brand; key topics: logo, pattern); specs (2 docs, subcategories: business; key topics: partnership, use case); code-conventions (1 doc, subcategories: code-style; key topics: anti-pattern).
domain: library
status: active

# Capabilities this repo is authoritative for — the sharpest routing
# signal. Seeded from the repo name; sharpen these by hand.
owns: [spec-driven-development, knowledge-base-generation, knowledge-gardening, bedrock-knowledge-sync, repo-manifest, knowledge-classification]
# Keywords a task might mention that point here (seeded from knowledge categories).
topics: [sdd-knowledge, garden, constitution, frontmatter, classification, tf-idf, bm25, diataxis, bedrock, s3-sync, learnings, repo-manifest, speckit]

# Repos/teams that depend on this one (curated — fill in).
consumers: [conductor, trustwallet-org-repos-running-knowledge-sync]
# Internal repos this one depends on (auto-detected from shared npm scope).
depends_on: []

# Artifacts this repo ships, and its tech stack (auto-detected).
ships: []
stack: [Go]

generatedBy: sdd-knowledge
verified: 2026-06-14
